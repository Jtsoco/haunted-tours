<%= simple_form_for([tour, review], data: {
  action: 'submit->insert-in-list#submit'}) do |f| %>
  <%= f.input :comment, label: 'Comment' %>
  <%= f.button :submit, class: 'create-review'%>
<% end %>
